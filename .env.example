# Environment variables — loaded by `python-dotenv` in every notebook:
#
#   ----------------------------------------------------------------
#    from dotenv import load_dotenv
#    load_dotenv()
#    PROJECT_ROOT = Path(os.getenv("PROJECT_ROOT", r"<default>"))
#   ----------------------------------------------------------------
#
# Copy this file to .env and fill in your values.
# DO NOT COMMIT .env TO VERSION CONTROL.

# ── Project paths ────────────────────────────────────────────────────────────

# Non-sensitive repo root (OneDrive/GitHub — committed/synced)
PROJECT_ROOT=C:\Users\jamesr4\OneDrive - Memorial Sloan Kettering Cancer Center\Documents\GitHub\llm_summarization_br_ca

# Sensitive/raw data — local only, NEVER committed or synced to OneDrive/GitHub
# Contains: raw PDFs (PHI), deidentified PDFs, extracted text, case mappings
DATA_PRIVATE_DIR=C:\Users\jamesr4\loc\data_private

# ── API Keys ─────────────────────────────────────────────────────────────────

OPENAI_API_KEY=your-openai-api-key
ANTHROPIC_API_KEY=your-anthropic-api-key
PHOENIX_API_KEY=your-phoenix-api-key